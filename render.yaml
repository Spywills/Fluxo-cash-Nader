# Render.com configuration
services:
  # Backend FastAPI
  - type: web
    name: fluxocash-backend
    env: docker
    region: oregon
    plan: free
    branch: main
    envVars:
      - key: DEBUG
        value: False
    healthCheckPath: /health
